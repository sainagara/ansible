- name: install packages
  hosts: frontend
  become: yes
  tasks:
  - name: print installing packages
    debug:
      msg: "Installing {{item}}"
    package:
      name: "{{item}}"
      state: present
    loop:
    - nginx
    - mysql-server
    - nodejs